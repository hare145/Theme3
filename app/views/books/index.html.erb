<%= render "users/user_info", user_info: @user %>

<%= link_to "edit", edit_user_path(current_user.id) %>

<%= render "new_book", book: @book %>

<h1>Books</h1>

<table>
  <tr>
   <th></th>
   <th>title</th> 
   <th>opinion</th>
  </tr>
  <% @books.each do |book| %>
  <tr>
   <td><%= image_tag book.user.get_profile_image(100, 100) %></td>
   
   <td><%= link_to book.title, book_path(book.id) %></td>
   
   <td><%= book.body %></td>
   
  </tr> 
  <% end %>
</table>